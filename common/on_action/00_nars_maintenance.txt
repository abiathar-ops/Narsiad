nars_one_time_maintenance_on_action = {
    trigger = {
        NOT = { has_character_flag = nikean_initialized }
		is_alive = yes
		is_ethnically_nikean = yes
    }
    effect = {
        add_character_flag = nikean_initialized
		nars_add_nikean_trait_on_start = yes
    }
}
