namespace = nikean_test

nikean_test.0011 = {
	type = character_event
	title = nikean_test.0011.t
	desc = nikean_test.0011.desc
	theme = emperor
	override_background = bp2_study_indian
	override_effect_2d = { reference = smoke }

	left_portrait = {
		character = scope:pugilist
		animation = map_dismissal
		camera = camera_event_scheme_left
	}
	right_portrait = {
		character = scope:savant
		camera = camera_event_scheme_left
		animation = thinking
	}

	trigger = {
		exists = capital_province
	}

	immediate = {
		hidden_effect = {
			#────────────────────────────────────────────────
			# PUGILIST 
			#────────────────────────────────────────────────
			random_pool_character = {
				province = capital_province
				limit = {
					martial >= high_skill_rating
					prowess >= high_skill_rating
					learning >= high_skill_rating
					is_adult = yes
					is_alive = yes
					culture = { has_cultural_pillar = heritage_nars }
				}
				alternative_limit = {
					martial >= medium_skill_rating
					prowess >= medium_skill_rating
					learning >= medium_skill_rating
					is_adult = yes
					is_alive = yes
					culture = { has_cultural_pillar = heritage_nars }
				}
				weight = {
					base = 1
					modifier = { faith = root.faith factor = 10 }
				}
				save_scope_as = pugilist
			}
			if = {
				limit = { NOT = { exists = scope:pugilist } }
				create_character = {
					location = root.capital_province
					culture = root.culture
					faith = root.faith
					gender_female_chance = 50
					martial = { 15 30 }
					prowess = { 15 30 }
					learning = { 10 16 }
					age = { 25 50 }
					save_scope_as = pugilist
					after_creation = {
						add_trait = nars_naturalis
						}
				}
			}
			scope:pugilist ?= {
				random = {
					chance = 15
					if = {
						limit = { NOT = { has_trait = lifestyle_blademaster } }
						add_trait = lifestyle_blademaster
					}
				}
				random = {
					chance = 15
					if = {
						limit = { NOT = { has_trait = strong } }
						add_trait = strong
					}
				}
				random = {
					chance = 15
					if = {
						limit = { NOT = { has_trait = physique_good_3 } }
						add_trait = physique_good_3
					}
				}
				random = {
					chance = 15
					if = {
						limit = { NOT = { has_trait = beauty_good_3 } }
						add_trait = beauty_good_3
					}
				}
			}

			#────────────────────────────────────────────────
			# SAVANT — high learning
			#────────────────────────────────────────────────
			random_pool_character = {
				province = capital_province
				limit = {
					learning >= high_skill_rating
					stewardship >= high_skill_rating
					intrigue >= high_skill_rating
					is_adult = yes
					is_alive = yes
					NOT = { scope:pugilist ?= this }
					culture = { has_cultural_pillar = heritage_nars }
				}
				alternative_limit = {
					learning >= medium_skill_rating
					stewardship >= medium_skill_rating
					intrigue >= medium_skill_rating
					is_adult = yes
					is_alive = yes
					culture = { has_cultural_pillar = heritage_nars }
					NOT = { scope:pugilist ?= this }
				}
				weight = {
					base = 1
					modifier = { faith = root.faith factor = 10 }
				}
				save_scope_as = savant
			}
			if = {
				limit = { NOT = { exists = scope:savant } }
				create_character = {
					location = root.capital_province
					culture = culture:nars_nikean
					faith = faith:nars_religion
					gender_female_chance = 50
					learning = { 15 30 }
					stewardship = { 15 30 }
					intrigue = { 15 30 }
					age = { 25 50 }
					save_scope_as = savant
					after_creation = {
						add_trait = nars_naturalis
						}
				}
			}
			scope:savant ?= {	
				random = {
					chance = 15
					if = {
						limit = { NOT = { has_trait = scholar } }
						add_trait = scholar
					}
				}
				random = {
					chance = 15
					if = {
						limit = { NOT = { has_trait = shrewd } }
						add_trait = shrewd
					}
				}
				random = {
					chance = 15
					if = {
						limit = { NOT = { has_trait = intellect_good_3 } }
						add_trait = intellect_good_3
					}
				}
				random = {
					chance = 15
					if = {
						limit = { NOT = { has_trait = beauty_good_3 } }
						add_trait = beauty_good_3
					}
				}
			}
		}
	}

	option = {
		name = nikean_test.0011.a
		trigger = {
			is_playable_character = yes
			exists = scope:pugilist
		}
		pay_treasury_or_gold = {
			target = scope:pugilist
			value = major_treasury_or_gold_value
		}
		add_courtier = scope:pugilist
		ai_chance = {
			factor = 100
			modifier = {
				short_term_gold < medium_gold_value
				factor = 0
			}
		}
	}

	option = {
		name = nikean_test.0011.b
		trigger = {
			is_playable_character = yes
			exists = scope:savant
		}
		pay_treasury_or_gold = {
			target = scope:savant
			value = major_treasury_or_gold_value
		}
		add_courtier = scope:savant
		ai_chance = {
			factor = 100
			modifier = {
				short_term_gold < medium_gold_value
				factor = 0
			}
		}
	}

	option = {
		name = {
			trigger = {
				NOR = {
					exists = scope:pugilist
					exists = scope:savant
				}
			}
			text = nikean_test.0011.d.none
		}
		name = {
			trigger = {
				OR = {
					exists = scope:pugilist
					exists = scope:savant
				}
			}
			text = nikean_test.0011.d.decline
		}
		custom_tooltip = nikean_test.0011.d.tt
		ai_chance = { factor = 2 }
	}
}